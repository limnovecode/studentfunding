= form_for([:adminboard, @legal]) do |f|
  - if @legal.errors.any?
    #error_explanation
      %h2 #{pluralize(@legal.errors.count, "error")} prohibited this legal from being saved:
      %ul
        - @legal.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :active
    = f.check_box :active
  .actions
    = f.submit "Save"
